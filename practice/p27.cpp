// kickstart
#include<bits/stdc++.h>
#include<vector>
using namespace  std;
int main()

{
    int n; scanf("%d",&n);
    int a,b;
    scanf("%d",&a);
    vector<int> v;
    while(a--)
    {
        scanf("%d",&b);
        v.emplace_back(b); 
    }
    int c,d;
    cin>>c;
    while(c--){
        scanf("%d",&d);
        v.emplace_back(d);
    }

    
    if(x==n) printf("I become the guy.");
    else printf("Oh, my keyboard!");
    return 0;
}    
   

